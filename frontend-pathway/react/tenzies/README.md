# Tenzies Game

An interactive dice game where players roll to match all numbers, featuring accessibility enhancements for keyboard users.

## 🛠️ Built With
- **React**: Developed a dynamic game using `useState` for die values, `useEffect` for win confetti effect, and `useRef` for focus management.
- **CSS**: Created a custom grid layout for the dice with conditional styling for the "held" state.

## 🎮 Key Features
- **Accessibility**: Real-time announcements for screen readers and automatic keyboard focus management.
- **Game Logic**: Ability to freeze specific dice between rolls to reach a winning state.
- **Interactive UI**: Responsive dice elements with smooth transitions.